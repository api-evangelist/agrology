openapi: 3.2.0
info:
  version: 1.0.0
  title: Agrology Public API v2 Summary Data API
servers:
- url: https://api.agrology.ag/v2
tags:
- name: Summary Data
paths:
  /summaries/microclimate/customer/{customerID}:
    get:
      parameters:
      - name: customerID
        in: path
        required: true
        description: The CustomerID value of the ReportDefinition
        schema:
          type: string
      security:
      - publicApiAuthorizer: []
      responses:
        '200':
          description: 200 response
          content:
            application/json:
              schema:
                type: string
      tags:
      - Summary Data
      summary: Get summaries microclimate customer by customer id
      x-summary-source: derived
      operationId: getSummariesMicroclimateCustomerByCustomerID
      x-operation-id-source: derived
components:
  securitySchemes:
    publicApiAuthorizer:
      type: http
      scheme: bearer
      bearerFormat: JWT
